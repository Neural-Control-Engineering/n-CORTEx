function updateExtractionLog(params, modality, sessionName)
    % given path to extraction log, as determined by projectDir and
    % modality, write a new session into the extraction log, initialized as
    % 0
end